checkoutrepo.yml 
----------------- 
 name: Chekout code
 on: push 
 jobs: 
   checkout-job:
      runs-on: ubuntu-latest 
	  steps: 
	    - name: checkout repo 
		  uses: action/checkout@v4   #under action/ checkout-version4 - predefined  #checkout is function , it will clone the code automatically
		- name: print all files 
		  run: ls -la 