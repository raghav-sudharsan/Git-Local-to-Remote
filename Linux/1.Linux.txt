Booting process
===============

bios/uefi-post    #hardware level
MBR/GPT Loading   #grup #disk virtualization
grup --bootloader --load the kernal  #AMI  #till this steps amazon wont allow you to modify
kernal initialization
system
target level

login/prompt:
-===========
$ #normal user
# #sudo user 

boot loader
===========
.cfg - configuration
systemd - system demon  #first process from boot #process id 1

who
whoami
who-r
who-dmesg



#aws
cloud init or user data  #instance data and automation

instance log
systemctl --failed
uptime   #monitor the up time
last reboot

view boot logs:
===============
journalctl -b 
dmesg
systemd

lifecyclehooks
================

pre-boot ---- ec2 Imge builder tool ---- customize AMI

firstboot --- userdata/cloud-init

systemctl
earlyboot ---- ec2 medata service

kernel/system ----- log boot issues ---- journalctl

appstart -- post-boot ----- ansible/codeploy/ssm agent


lsof       #to see current open files
lsns


22/10/25 - Linux -03
=====================

 
 date +"%H" -02   #I,j,m,M
 
#profile
---------
 user level
 kernal level
 
 ls -l #long list
 ls -a #hidden files/dir
 
 /home  #home directory for the normal user
 /root  #home directory for the root user
 /ect   #contains config files
 
#commands type
----------------
 type .  #check the type of .
 type pwd
 type ls
 
 cd .
 cd ..
 cd ~
 
 w        #to check logged user details and its process
 man w
 tty      # terminal session details
 uname -r
 uname -a
 
 cat /etc/os-release   # OS details
 cat /etc/os-release | head -n2
 cat /etc/os-release | tail -n3
 
 !3   #call from the history 3rd commands
 

23/10  Module -1 Introduction to file system
======================================================
file types:
 ----------
  regular file  : -   #ls -l
  directory file : d
  device file/block: b
  socket            :s
  name pipes        :p
  character files   :c
  
common linux file system types
===============================

 ex2  - journaling              #used in legacy server
 ext3
 ext4 - extension 4             #using current 
 xfs  - extended file system    #use for Enterprise
 btrfs-                         #used to store the snapshots
 vfat - virtual file allogation table 
 
 tempfs -            #temperory in memory file system
 ntfs  - network file system
  

$
/      #indicates begening of the commands
/bin   #
/boot  #bootloader files
/dev   #  - device releated files   /dev/null
/etc   #configuration files
/home  #
/lib   #
/media #removable device
/opt   #third party software
/proc  #virtual file system describe the process and kernel info 
/sbin  #admin user commands
/bin   #normal user commands (common commands
/tmp
/usr   #user releated files like documents, library,...
/var   #logs and cache purpose, use this dir
/mnt   #mount points
/srv   #services files
/root  #home dir for the root user
/sys   #kernal informations

important:
 /bin
 /boot
 /dev
 /etc
 /proc
 
 
24/10- file system:
===================
 disk space allogated as blocks
 512 bytes - 2048
 
 q:how to find block size on ypur files system?
 
 types of block
 ----------------
 1.boot block  - used to boot the host machine   #contain bootstrap loader
 2.super block  - state of the file system
 3.inode block/table: ls -la ls -i
 4.data block - 
  uuid--ext4,xfs
 

Commands:
----------
 df -Th
 du -sh /var/log    #-sh summary human readable format
 lsblk - 
 blkid
 man blkid
 sudo blkid
 sudo file -s /dev/xvda128
 sudo file -s /dev/xvda
 mount | grep /dev   #"|" - pipe command "greo" used to search
  
 man tune2fs
 sudo tune2fs -l /dev/xvda1
 man xfs_info
 xfs_info /
 xfs_db -r /dev/xvda1   #db level commands 
 
 man fsck    #change and repair a linux file system
 sudo fsck /dev/xvda1
 sudo cat /etc/fstab
 df -i
 ls -li
 
 
 
27/10 - file related commands
===============================

 touch   - create empty file/update timestamp of the file
 cat    - to view the file
 tac    - 
 cp
 mv
 rename
 mkdirrmdir
 
 
touch  #create a empty file  *Mainly used to timestamp without changes in file data
-----
  touch f1
  touch f2 f3 f4
  touch f{5..200}       #to create n no.of files
 
 ls -l f1
 touch f1           #to update time stapm
 ls -l f1           #time stamp will be updated
 rm -rf *           #remove all the files
 
 mkdir mydir
 touch mydir/demo.txt
 cd mydir/
 ls -l demo.txt
 cd ~

timestamps:
------------
 1. atime - last access time        #read/viewed
 2. mtime - last modification time  #content changes
 3. ctime - last status change      #permission, owners
 
  date -u
  date 
  timedatectl
  timedatectl -timezone
  timedatectl list-timezones | grep Asia/Calcutta
  sudo timedattectl set-timezone Asia/Calcutta
  date
  
 
 
27/10 - cp: Copy
=================
$cp source dir dest dir

 mkdir test
 cd test
 touch myfile.txt
 echo welcome to file operation
 
 ls -i myfile.txt       #to check inode id of the file
 cp myfiles.txt /opt
 
 lsof
 lsof | grep f1.txt
 ulimit
 
 mkdir -p d1/d2/d3/d4
 rm -r d1         #recursive delete for dir and file
 
cat
----
 cat new.txt
 cat dev > new.txt
 cat tst >> new.txt  #append the data to exist file
 tac new.txt         #
 
 
To find the file types:
-------------------------
 ls -l /* | grep "^s"   #to get symbolic files
 ls -l /* | grep "^-"   #to get empty file
 ls -l /* | grep "^b"   #to get block file
 
 file man    #to determine the types
 file new
 file demo
 
 stat demo/
 
 
 
 q:you deploying webapp to /var/www/html using cp -r ----nginx -- 403
 2. cp -r-millions of files, find solution
 
 
========================================================================================
----------------------------BASICS COMMANDS OF LINUX------------------------